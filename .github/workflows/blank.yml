name: workflow-1
on: workflow_dispatch
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print hello
        run: echo "Hello"
      - name: Print a greeting and bye
        run: |
            echo "Good Morning"
            echo "BYEEEE"
